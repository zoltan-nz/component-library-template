dist: xenial
language: node_js

node_js:
  - "8"
  - "10"
  - "11"
  - "12"

install:
  - yarn

addons:
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable

matrix:
  fast_finish: true

cache:
  npm: true
  yarn: true
  directories:
    - node_modules

script:
  - yarn build:all
  - yarn test:acceptance:react:setup
  - yarn test
